# ⚡ Быстрый старт

## 🚀 Запуск за 5 минут

### 1. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 2. Запуск системы
```bash
python run.py
```

### 3. Открытие в браузере
Перейдите по адресу: **http://localhost:5000**

### 4. Использование
1. Нажмите "📹 Запустить камеру"
2. Нажмите "🚀 Начать анализ"
3. Следите за обнаруженными объектами и событиями

## 🔧 Быстрая настройка

### Изменение порога уверенности
В файле `config.py`:
```python
CONFIDENCE_THRESHOLD = 0.7  # Увеличьте для меньшего количества ложных срабатываний
```

### Включение email уведомлений
В файле `config.py`:
```python
ENABLE_EMAIL_ALERTS = True
EMAIL_USER = 'your-email@gmail.com'
EMAIL_PASSWORD = 'your-app-password'
```

## 🧪 Тестирование

```bash
# Запуск тестов
python test_system.py

# Примеры использования API
python api_examples.py
```

## 📱 API для разработчиков

```python
import requests

# Запуск анализа
requests.post('http://localhost:5000/api/start_analysis')

# Получение статуса
requests.get('http://localhost:5000/api/status').json()

# Получение событий
requests.get('http://localhost:5000/api/events').json()
```

## 🆘 Если что-то не работает

1. **Проверьте камеру**: `python -c "import cv2; print(cv2.VideoCapture(0).isOpened())"`
2. **Переустановите зависимости**: `pip install --upgrade -r requirements.txt`
3. **Проверьте логи**: Откройте консоль браузера (F12)

## 📞 Поддержка

- 📖 Полная документация: `README.md`
- 🚀 Развертывание: `DEPLOYMENT.md`
- 🧪 Тесты: `test_system.py`
- 📝 Примеры API: `api_examples.py`
