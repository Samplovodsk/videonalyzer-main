# 🔍 Система автоматического анализа видеопотока

**Курсовая работа на тему: "РАЗРАБОТКА СИСТЕМЫ АВТОМАТИЧЕСКОГО АНАЛИЗА ВИДЕОПОТОКА ДЛЯ ОБНАРУЖЕНИЯ ПОДОЗРИТЕЛЬНЫХ ДЕЙСТВИЙ"**

## 📋 Описание проекта

Веб-приложение с машинным обучением для автоматического анализа видеопотока с камер видеонаблюдения на предмет обнаружения подозрительных действий и нарушений безопасности.

### 🎯 Основные возможности

- **Детекция объектов в реальном времени** с использованием YOLO v8
- **Анализ поведения** для выявления подозрительных действий
- **Система уведомлений** о нарушениях безопасности
- **Веб-интерфейс** для мониторинга и управления
- **База данных** для хранения событий безопасности
- **API** для интеграции с другими системами

### 🔍 Типы обнаруживаемых нарушений

- **Подозрительная близость** - человек слишком близко к камере
- **Обнаружение оружия** - ножи, пистолеты
- **Подозрительные объекты** - бутылки, сумки, телефоны
- **Необычное поведение** - анализ движения и паттернов

## 🚀 Быстрый старт

### Установка зависимостей

```bash
# Клонируйте репозиторий
git clone <repository-url>
cd videoanalyzer

# Установите зависимости
pip install -r requirements.txt
```

### Запуск приложения

```bash
# Запуск через основной скрипт (рекомендуется)
python run.py

# Или напрямую через Flask
python app.py
```

### Доступ к приложению

Откройте браузер и перейдите по адресу: **http://localhost:5000**

## 📁 Структура проекта

```
videoanalyzer/
├── app.py                 # Основное Flask приложение
├── run.py                 # Скрипт запуска с проверками
├── config.py              # Конфигурация системы
├── video_processor.py     # Обработка видеопотока
├── notification_system.py # Система уведомлений
├── requirements.txt       # Зависимости Python
├── templates/
│   └── index.html        # Веб-интерфейс
└── README.md             # Документация
```

## 🔧 Конфигурация

Настройки системы находятся в файле `config.py`:

```python
# Основные настройки
YOLO_MODEL = 'yolov8n.pt'           # Модель YOLO
CONFIDENCE_THRESHOLD = 0.5          # Порог уверенности
PROXIMITY_THRESHOLD = 0.7           # Порог близости к камере

# Подозрительные классы объектов
SUSPICIOUS_CLASSES = {
    'person': 0,
    'knife': 43,
    'gun': 44,
    'bottle': 39,
    'cell phone': 67
}
```

## 🌐 API Endpoints

### Управление анализом

- `POST /api/start_analysis` - Запуск анализа видеопотока
- `POST /api/stop_analysis` - Остановка анализа
- `GET /api/status` - Получение статуса системы

### Работа с видео

- `POST /api/upload_frame` - Загрузка кадра для анализа
- `GET /api/get_results` - Получение результатов анализа

### События безопасности

- `GET /api/events` - Список всех событий
- `GET /api/event/<id>/frame` - Получение кадра события

## 💻 Использование веб-интерфейса

1. **Запуск камеры** - Нажмите "📹 Запустить камеру" для доступа к веб-камере
2. **Начало анализа** - Нажмите "🚀 Начать анализ" для запуска обработки
3. **Мониторинг** - Следите за статусом системы и обнаруженными объектами
4. **Просмотр событий** - Нажмите "📋 Загрузить события" для просмотра истории

## 🔍 Алгоритм работы

### 1. Захват видео
- Получение кадров с веб-камеры через WebRTC
- Кодирование в base64 для передачи на сервер

### 2. Обработка кадров
- Декодирование кадров
- Анализ с помощью YOLO v8
- Детекция объектов и их классификация

### 3. Анализ поведения
- Проверка размера и позиции объектов
- Анализ движения между кадрами
- Выявление подозрительных паттернов

### 4. Уведомления
- Сохранение событий в базу данных
- Отправка email уведомлений (опционально)
- Логирование всех событий

## 📊 Технические характеристики

### Производительность
- **FPS**: 1-5 кадров в секунду (настраивается)
- **Задержка**: < 2 секунд
- **Точность**: 85-95% для детекции людей

### Системные требования
- **Python**: 3.8+
- **RAM**: 4GB+ (рекомендуется 8GB)
- **GPU**: Опционально (CUDA для ускорения)
- **Камера**: Веб-камера или IP-камера

## 🛠️ Разработка

### Добавление новых типов нарушений

1. Обновите `SUSPICIOUS_CLASSES` в `config.py`
2. Добавьте логику в `analyze_behavior()` в `video_processor.py`
3. Обновите веб-интерфейс при необходимости

### Настройка уведомлений

```python
# В config.py
ENABLE_EMAIL_ALERTS = True
SMTP_SERVER = 'smtp.gmail.com'
EMAIL_USER = 'your-email@gmail.com'
EMAIL_PASSWORD = 'your-password'
```

## 📈 Возможности расширения

- **Интеграция с IP-камерами** через RTSP
- **Облачная обработка** с использованием AWS/Azure
- **Мобильное приложение** для уведомлений
- **Интеграция с системами безопасности**
- **Анализ звука** для обнаружения подозрительных звуков
- **Распознавание лиц** для идентификации людей

## 🐛 Устранение неполадок

### Проблемы с камерой
```bash
# Проверьте доступные камеры
python -c "import cv2; print(cv2.VideoCapture(0).isOpened())"
```

### Проблемы с моделью YOLO
```bash
# Переустановите ultralytics
pip uninstall ultralytics
pip install ultralytics
```

### Проблемы с производительностью
- Уменьшите разрешение камеры
- Увеличьте интервал между кадрами
- Используйте GPU для ускорения

## 📝 Лицензия

Этот проект создан в рамках курсовой работы. Используйте в образовательных целях.

## 👨‍💻 Автор

Студент группы [номер группы]
Курсовая работа по дисциплине [название дисциплины]

---

**Примечание**: Для продакшн использования рекомендуется дополнительная настройка безопасности и оптимизация производительности.

